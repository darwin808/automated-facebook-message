name: daily-cron
on:
   schedule:
      - cron: "0 3 * * *"
jobs:
   cron:
      runs-on: ubuntu-latest
      steps:
         - name: Call our API route
           run: |
              curl --request GET \
              --url 'https://peaceful-cove-89366.herokuapp.com/api/messenger?q=Good%20night%20😊' \
